import React from 'react'
import NavHeader from '../NavHeader/NavHeader'

const Tasks = (props) => {
    return (
        <div>
            <NavHeader/>
            Tasks
        </div>
    )
}

export default Tasks;